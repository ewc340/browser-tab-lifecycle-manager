/**
 * Settings view — fully implemented in Milestone 1.
 */
export function SettingsView() {
  return (
    <div className="placeholder-view">
      <h2 className="placeholder-view__title">Settings</h2>
      <p className="placeholder-view__message">
        Full settings controls — sleep and close thresholds, host rules, theme, and shortcuts —
        arrive in Milestone 1.
      </p>
    </div>
  );
}
