/**
 * Recovery list — arrives in Milestone 3.
 */
export function RecoveryView() {
  return (
    <div className="placeholder-view">
      <h2 className="placeholder-view__title">Recovery</h2>
      <p className="placeholder-view__message">
        The recovery list — letting you restore automatically closed tabs — arrives in Milestone 3.
      </p>
    </div>
  );
}
