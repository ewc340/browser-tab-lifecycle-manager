/**
 * Activity feed — arrives in Milestone 3.
 */
export function ActivityView() {
  return (
    <div className="placeholder-view">
      <h2 className="placeholder-view__title">Activity</h2>
      <p className="placeholder-view__message">
        The activity feed — showing automatic sleep and close events with reasons — arrives in
        Milestone 3.
      </p>
    </div>
  );
}
